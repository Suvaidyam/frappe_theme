name: Deprecated – Frappe Tests (use CI – Pipeline)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
        - run: |
          echo "This workflow is deprecated and will not run Frappe tests."
          echo "Please use the main CI – Pipeline workflow (.github/workflows/ci.yml) which supports the required Python version for Frappe."
